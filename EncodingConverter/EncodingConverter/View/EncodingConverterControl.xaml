<UserControl x:Class="EncodingConverter.View.EncodingConverterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EncodingConverter.View"
             xmlns:resources="clr-namespace:EncodingConverter.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10">
        <Label Content="{Binding EncodingText, Mode=TwoWay}"/>
        <Label Content="Available encodings:"/>
        <ListBox ItemsSource="{Binding EncodingCollection}" SelectionMode="Single">
            <ListBox.ItemTemplate> 
                <DataTemplate>
                    <CheckBox IsChecked="False" Content="{Binding EncodingName}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 10 0 0">
            <Button Content="Convert" Margin=" 0 0 10 0" Command="{Binding ConvertCommand}"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
